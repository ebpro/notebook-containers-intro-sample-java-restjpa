/**
 * Configuration classes.
 *
 * The config package contains classes responsible for application
 * configuration,
 * such as setting up dependency injection, database connections, and other
 * infrastructure-related settings.
 * In a typical hexagonal architecture, this package helps wire together the
 * various layers of the application,
 * ensuring that ports and adapters are correctly connected.
 *
 * In an application server environment, this would be automatically managed by
 * the server,
 * but in a standalone Java SE application, we need to handle this manually.
 *
 */
package fr.univtln.bruno.demos.docker.config;
