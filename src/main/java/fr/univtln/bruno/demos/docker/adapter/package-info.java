/**
 * Package grouping adapters.
 *
 * In this layered architecture, adapters are responsible for communication
 * between the application core (domain and application layers) and the
 * outside world (e.g., databases, web services, messaging systems).
 */
package fr.univtln.bruno.demos.docker.adapter;
